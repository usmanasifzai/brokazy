import {StyleSheet} from 'react-native';

export const makeStyles = () =>
  StyleSheet.create({
    input: {
      height: 40,
      borderBottomWidth: 1,
      borderColor: 'gray',
      color: '#fff',
      marginTop: 20,
    },
  });
